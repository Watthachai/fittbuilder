# Test script — ข้อเสนอโครงการ + ซ่อมตัวแคป (v0.51.0)

คู่กับ `docs/jira/2026-08-24-proposal-and-capture.md`

## ความเสี่ยง → เคสทดสอบ

| # | ความเสี่ยง | เคส | เกณฑ์ผ่าน (วัดได้) | สถานะ |
|---|---|---|---|---|
| 1 | พิมพ์เร็วกว่ารูปโหลด | เปิดแท็บใหม่ (cache เย็น) → ใบเสนอราคา → พิมพ์ทันที | รูปภาคผนวกออก **ครบทุกใบตั้งแต่รอบแรก** | พิสูจน์ระดับ unit แล้ว (6 เคส) — ต้องยืนยันผ่าน UI |
| 2 | ลิงก์รูปหมดอายุ 8 ชม. | mock: แก้ `sheet` ให้ถือ URL หมดอายุแล้วกดพิมพ์ | มีการเรียก `listShots` ใหม่ก่อน mount ทุกครั้ง (โค้ดบังคับเส้นทางนี้) | โดยโครงสร้าง |
| 3 | รูปเสีย 1 ใบล็อกทั้งเอกสาร | เพิ่ม `<img>` ที่ 404 ในกระดาษ | พิมพ์ได้ ใบพังพิมพ์เป็นกรอบว่าง ไม่เกิน timeout 8 วิ | unit ผ่าน |
| 4 | สแกนอัตโนมัติไม่บันทึกเส้นทาง | รีสตาร์ทพรีวิว → สแกนอัตโนมัติ → เปิดผัง Flow | มีลูกศร ≥1 เส้นพร้อมป้ายชื่อปุ่ม โดยไม่ได้อัดเอง | **ยังไม่พิสูจน์ — ต้องเดินบนโปรเจกต์จริง** |
| 5 | แต่งเส้นทางที่ไม่มีใครเดิน | ดูภาพแรกของการสแกน (หน้าเปิดแอป) | ต้อง **ไม่มี** edge เข้า (via = null) | unit ผ่าน (`stepJourney` คืน null) |
| 6 | `[object Event]` | สร้างเดโมที่มี `<img>` ชี้โดเมนที่อ่านไม่ได้ → แคปทุกหน้า | ข้อความ error ระบุจำนวนรูป + URL ตัวอย่าง ไม่ใช่ `[object Event]` | **ยังไม่พิสูจน์ผ่าน UI** |
| 7 | modal "ยกเลิกรายการ" ปิดไม่ได้ | เดโมที่ modal มีแต่ปุ่มป้ายยาว | แคปต่อ modal ถัดไปของหน้าเดิมได้ ไม่ขึ้น "ข้าม modal ที่เหลือ" | **ยังไม่พิสูจน์ผ่าน UI** |
| 8 | กดปุ่มปิดผิดจนข้อมูลเปลี่ยน | ตรวจ regex `DESTRUCTIVE` | "ยกเลิกและลบ" / "cancel & save" ไม่ถูกกด (ไม่อยู่ในผลของ `closers`) | โดยโครงสร้าง — regex ตัดก่อนเข้า list |
| 9 | ปุ่มดัชนีหายหลังกด | โปรเจกต์ที่ดัชนีครบแล้ว | ปุ่มยังอยู่ ป้ายเป็น "ตรวจดัชนีอีกครั้ง" | โค้ดตัดเงื่อนไขซ่อนออกแล้ว |
| 10 | สองใบบอกจำนวนวันไม่ตรงกัน | แก้ days ในใบเสนอราคา → เปิดข้อเสนอโครงการ | ตัวเลข "พัฒนา N วันทำการ" เปลี่ยนตามทันที ไม่มีช่องพิมพ์เอง | unit ผ่าน (`proposalTimeline` อ่านจาก `quoteTotals`) |
| 11 | คำอธิบายหน้าจอสองชุดขัดกัน | เขียน note ในใบเสนอราคา → ดู step ในข้อเสนอ | ข้อความเดียวกัน (join ด้วยชื่อหน้าจอ) | unit ผ่าน |
| 12 | AI ทับข้อความคน | พิมพ์ context เอง → กดร่าง AI | context เดิมอยู่ครบ AI เติมเฉพาะส่วนว่าง | โค้ดบังคับ (`wantContext/wantPoints/wantExcluded`) |
| 13 | AI ตอบครึ่งใบ | mock ตอบ point ไม่มี problem | ข้อนั้นหาย ไม่พิมพ์ช่องว่าง | unit ผ่าน (11 เคส `proposal-write`) |
| 14 | ใบเก่าเปิดแล้วพัง (jsonb เสีย) | ป้อน payload พังทุกแบบเข้า `parseProposal` | คืน null → seed ใหม่ ไม่ crash | unit ผ่าน (20 เคส `proposal`) |
| 15 | field หายแจก white-label | payload ไม่มี `brand.poweredBy` | default = true (มี Powered by) | unit ผ่าน |
| 16 | RLS ตารางใหม่รั่ว | query `pg_policies` บน DB จริง | 3 นโยบาย SELECT/INSERT/UPDATE · UPDATE มีทั้ง using + with check · RLS enabled | **ผ่านแล้ว — ตรวจบน DB จริง 24 ส.ค.** |

## สิ่งที่พิสูจน์แล้ว

- Gate เต็มทุก commit: `tsc` · eslint · vitest **395 เคส** · `next build`
- Migration 0039 ลง DB จริง (แชร์ prod) — ตาราง+RLS+trigger ตรวจด้วย query แล้ว
- พฤติกรรมรอรูป/timeout/404 (unit) · การเรียง/join/timeline ของ proposal (unit) · parser AI (unit)

## สิ่งที่ยังไม่พิสูจน์ (ต้องเดินบน UI จริง)

1. เคส 1: พิมพ์รอบแรกหลัง cache เย็น — คนกดต้องเห็นเอง
2. เคส 4, 6, 7: ต้องรีสตาร์ทพรีวิว (bridge v21) แล้วเดินบนโปรเจกต์ JAS SOFA ที่พบอาการ — ตัวเลข "6 ไม่สำเร็จ" ควรลดลง แต่**ไม่รับประกันเป็นศูนย์**: หน้า/modal ที่เดโมเขียนมาผิดจริงยังพังต่อ ซึ่งตอนนี้ข้อความจะบอกแล้วว่าพังเพราะอะไร
3. กระดาษข้อเสนอบนเครื่องพิมพ์จริง (⌘P) — เลย์เอาต์ 2 คอลัมน์, break-inside, หัวกระดาษซ้ำหน้า 2
